package by.belzhd.android.tickectchecker.utils;

public class RequestInExpress {

    public static boolean sendRequest(boolean type, String file_1, String file_2)
    {
        int error = 0,ext = 0;
        //    io-express(type,file_1,file_2,error,ext)
        if(ext<2)
            return true;
        else
            return false;
    }
}
